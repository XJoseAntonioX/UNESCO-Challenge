{
  "name": "verifibot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "validate:artifact": "node -e \"import('./dist/server/index.js').then(m=>{if(!m.default?.fetch)process.exit(1)})\""
  }
}
